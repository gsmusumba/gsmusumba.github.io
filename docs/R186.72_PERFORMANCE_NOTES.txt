GS MUSUMBA R186.72 — STATIC TRANSFER SIZE NOTES

Approximate gzip sizes (Cloudflare may serve Brotli, often smaller):
- index.html: raw 11.0 KiB; gzip 4.0 KiB
- r18661-login.css: raw 2.8 KiB; gzip 1.1 KiB
- r18661-loader.js: raw 2.3 KiB; gzip 1.1 KiB
- r186-core.js: raw 429.6 KiB; gzip 54.5 KiB
- r18661-app.css: raw 794.1 KiB; gzip 124.6 KiB
- r18661-extensions.js: raw 1863.0 KiB; gzip 460.3 KiB

Loading design:
- Login shell loads first.
- Heavy app CSS/JS loads after authentication.
- Save-Data / 2G avoids speculative prefetch and loads heavy assets sequentially with one retry.
- Supabase/API data is not service-worker cached.

Performance truth:
- Cached repeat visits should be much faster than first post-login load.
- First post-login load on real 2G can still be slow because the current legacy extensions bundle is large; future code-splitting is a separate optimization and should only be done with full regression QA.
