GS MUSUMBA R186.74 — DEPLOYMENT STEPS

1. Back up the current R186.73 working folder / Git commit.
2. Replace the project files with this full R186.74 package; do not mix individual R186.73/R186.74 JS/CSS files.
3. Local static check:
   - index.html opens
   - login screen is styled
   - no missing local asset in DevTools Network.
4. Git:
   git status --short
   git add -A
   git diff --cached --check
   git commit -m "R186.74 district dashboard polish final"
   git push origin main
5. Cloudflare Pages:
   - confirm automatic deployment from main
   - confirm green success
   - confirm production domain https://musumba.pages.dev/
6. Hard refresh once after deployment (Ctrl+Shift+R).
7. Live smoke test in this order:
   login -> Teacher Dashboard -> Teacher Attendance hub -> Student Identification -> Marks -> Super Admin Dashboard -> DOS Dashboard -> printing/export -> phone/mobile data.
8. Test at least one Teacher account that is a Class Teacher and one that is not a Class Teacher.
9. Test phone on real mobile data. Record actual cold and repeat timings; do not infer 2G performance from desktop Wi-Fi.
