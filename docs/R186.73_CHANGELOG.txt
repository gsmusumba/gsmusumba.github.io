GS MUSUMBA R186.73 — DISTRICT COMPETITION FINAL

CHANGED
- Login: direct registered-email path skips identifier-resolution RPC; username/staff-code/telephone still use canonical resolver and remain supported.
- Login: authenticated shell/dashboard is shown immediately after secure bootstrap; heavy service CSS/JS continues in background instead of blocking role entry.
- Loader: service CSS and JS load in parallel on all links after authentication; Save-Data/2G still disables speculative prefetch.
- Service worker: returning navigation serves cached shell immediately and revalidates in background; Supabase/auth/API responses remain uncached.
- New small r18673-critical.css provides a professionally styled authenticated shell before the full app stylesheet arrives.
- Login/recovery UI made smaller, consistent and professional.
- Visible Loading text removed from core route transition and R186.72 attendance-monitor initial rows.
- Super Admin/role dashboard preview panels hidden from normal production workflow.
- Internal success/catalog noise banners hidden; warnings/errors remain visible.
- Dashboard/service cards compacted and standardized with controlled accent colors.
- Attendance NOT CALLED red restored to strong readable red with white text.
- Class and Subject Attendance Monitoring reduced to 12 essential on-screen columns; full XLSX export retains extended values.
- Attendance No. column fixed near 1 cm / 38 px; monitoring fits normal laptop content width.
- Forms, buttons, title hierarchy and system typography standardized to Segoe UI / Arial.
- Existing attendance/marks bottom-left save workflows preserved.
- Generic EDIT/UPDATE/CORRECT actions receive safe post-click editor focus when a visible editable field exists.
- Pages with print controls and a visible table can receive a generic XLSX export assist when the existing system XLSX exporter is available.
- Service-worker registration/version aligned to R186.73.

PRESERVED
- Supabase Auth, RLS, existing write RPCs and role permissions.
- Existing R186.71 global printing authority and class-teacher authority.
- Marks formulas, attendance write RPCs, timetable rules, report-card authority and existing data.
- R186.72 rollback remains in Git/Cloudflare history.
